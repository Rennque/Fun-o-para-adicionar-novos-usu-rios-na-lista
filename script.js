function novosNomes(sub) {
  const nomes = ["Pedro", "José", "Aderbal", "Danilo", "Luisa", "Vitoria"];
  console.log(nomes);
}
/* let novaLista = ["Pedro", "José", "Aderbal", "Danilo", "Luisa", "Vitoria"];
  try {
    sub.forEach((subs) => {
      if (typeof subs !== "string") {
        console.log(`So pode ser adicionado nomes`);
      } else if (novaLista.includes(subs)) {
        console.log(`Desculpe mais esse nome ja existe`);
      } else {
        novaLista.push(subs);
      }
    });
  } catch (error) {
    console.error(`Ocorreu um erro`);
  }
  console.log(novaLista);
}

novosNomes("Nicolas", "Cintia", "Henrique"); */
